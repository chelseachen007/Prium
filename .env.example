# Server Configuration
SERVER_PORT=3001
# Note: For SQLite, use file:/path/to/db. In Docker, it's mapped to /app/data
DATABASE_URL=file:/app/data/dev.db

# Web Configuration
# The port on the host machine that maps to the web container
WEB_PORT=80
