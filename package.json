{
  "name": "rss-reader",
  "version": "1.0.0",
  "private": true,
  "description": "RSS Reader - Pure Frontend Application",
  "scripts": {
    "dev": "cd apps/web && pnpm dev",
    "build": "cd apps/web && pnpm build",
    "preview": "cd apps/web && pnpm preview",
    "lint": "cd apps/web && pnpm lint",
    "type-check": "cd apps/web && pnpm type-check"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
